[
  {
    "name": "scrape",
    "description": "Scrape a URL and return content with metadata, links, and images. Returns clean, LLM-ready markdown with YAML frontmatter containing rich metadata including title, description, author, Open Graph, Twitter Cards, word count, reading time, and categorized links.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "The URL to scrape"
        }
      },
      "required": ["url"]
    }
  }
]
