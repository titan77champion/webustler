name: webustler
image: mcp/webustler
type: server
meta:
  category: web
  tags:
    - web-scraping
    - cloudflare-bypass
    - markdown
    - llm
    - anti-bot
about:
  title: Webustler
  description: MCP server for web scraping that actually works. Extracts clean, LLM-ready markdown from any URL — even Cloudflare-protected sites.
  icon: https://raw.githubusercontent.com/drruin/webustler/main/images/icon.ico
source:
  project: https://github.com/drruin/webustler
  commit: COMMIT_HASH
config:
  description: |
    Webustler scrapes web pages and returns clean, token-optimized markdown with rich metadata.

    Features:
    - Anti-bot bypass with smart fallback
    - Cloudflare protection handling
    - Rich metadata extraction (Open Graph, Twitter Cards, etc.)
    - Internal/external link categorization
    - Word count and reading time
    - Table preservation
    - File download detection
  env:
    - name: TIMEOUT
      description: Request timeout in seconds
      default: "120"
      required: false
